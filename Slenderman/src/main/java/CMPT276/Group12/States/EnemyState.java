package CMPT276.Group12.States;

/**
 * The EnemyState enum represents the various states that an enemy can be in.
 * Currently, the enemy needs no states, so this enum is empty.
 */
public enum EnemyState {
    ACTIVE,
    FROZEN
}
